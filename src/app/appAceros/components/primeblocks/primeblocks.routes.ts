import { Routes } from '@angular/router';
import { BlocksComponent } from './blocks/blocks.component';

export const primebloksRoutes: Routes = [
        { path: '', component: BlocksComponent }
];
export default primebloksRoutes;
